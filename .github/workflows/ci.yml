name: ci-cd

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false                      # both services report; one failing doesn't cancel the other
      matrix:
        service: [backend, frontend]        # two parallel copies, one per service
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: ./${{ matrix.service }}
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt               # app deps (flask, etc.)
          pip install -r requirements-dev.txt           # test + lint tools (pytest, flake8)

      # Step order inside a job is sequential, so this gates pytest below:
      # if flake8 exits non-zero, the job stops here and pytest never runs.
      - name: Lint with flake8
        working-directory: ./${{ matrix.service }}
        run: flake8 .

      - name: Run pytest
        working-directory: ./${{ matrix.service }}
        run: pytest

  build-and-push:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        service: [backend, frontend]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./${{ matrix.service }}
          push: true
          platforms: linux/amd64
          tags: |
            freddiec1212/skyhigh-patchwork-${{ matrix.service }}:v2
            freddiec1212/skyhigh-patchwork-${{ matrix.service }}:${{ github.sha }}